import React from 'react';
import Container from '.';

function Loading() {
  return (
    <Container>
      <h1 className="loadingText">Carregando...</h1>
    </Container>
  );
}

export default Loading;
